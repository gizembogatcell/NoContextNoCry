# Story 1: Anonim Retro Board (Timer'lı)

**Status**: Approved
**Priority**: Must
**Epic**: Retro Board
**Created**: 14 Mayıs 2026

---

## Story

**As a** moderatör (Scrum Master / takım lideri),
**I want** anonim sticky not yazma aşamasını timer ile yönetebilmek ve timer dolunca tüm notların ekrana açılmasını sağlamak,
**so that** ekip üyeleri birbirinden etkilenmeden düşüncelerini özgürce paylaşabilsin ve retro yapılandırılmış şekilde ilerlesin.

---

## Acceptance Criteria

1. Moderatör yeni bir retro oluşturabilir; retro adı ve timer süresi (dakika cinsinden) girilir.
2. Ekip üyelerine paylaşılabilir bir retro linki oluşturulur; link ile katılan kullanıcılar anonim olarak tanımlanır (isim görünmez).
3. Katılımcılar Mad / Sad / Glad kolonlarına sticky not ekleyebilir; yazma aşamasında başkalarının notları görünmez.
4. Moderatör timer'ı başlatır; geri sayım tüm katılımcılara canlı olarak gösterilir.
5. Timer sıfırlandığında yazma aşaması otomatik kapanır ve tüm notlar tüm katılımcılara açılır.
6. Moderatör timer dolmadan da "Aşamayı Bitir" ile yazma aşamasını kapatabiliр.
7. Edge case: Katılımcı not yazmadan timer dolarsa, o kullanıcının notu olmaması normal kabul edilir; hata gösterilmez.

---

## Tasks / Subtasks

- [ ] Task 1: Retro oluşturma API + sayfası (AC: 1, 2)
  - [ ] Subtask 1.1: `POST /api/retros` endpoint — retro adı, timer süresi, oluşturan user ID alır; MongoDB'ye kaydeder
  - [ ] Subtask 1.2: Benzersiz retro ID ile katılım linki üretilir (`/retro/[id]`)
  - [ ] Subtask 1.3: Retro oluşturma formu (`src/app/(app)/retro/new/page.tsx`)
  - [ ] Subtask 1.4: Oluşturma akışı için testler yaz
- [ ] Task 2: Anonim katılım + sticky not ekleme (AC: 3)
  - [ ] Subtask 2.1: Retro'ya katılan kullanıcılar anonim ID ile session'a atanır (Firebase Auth anonymous sign-in veya random UUID)
  - [ ] Subtask 2.2: `POST /api/retros/[id]/notes` — kolona sticky not ekler (kategori: mad/sad/glad, içerik, anonim katılımcı ID)
  - [ ] Subtask 2.3: Yazma aşamasında başkasının notları frontend'de gizlenir (sadece kendi notları görünür)
  - [ ] Subtask 2.4: Sticky not ekleme için testler yaz
- [ ] Task 3: Timer mekanizması (AC: 4, 5, 6)
  - [ ] Subtask 3.1: Moderatör "Timer Başlat" butonuna basar; retro durumu `writing` olarak güncellenir, bitiş zamanı MongoDB'ye yazılır
  - [ ] Subtask 3.2: Frontend polling (5 sn) ile retro durumunu kontrol eder; timer geri sayımı gösterir
  - [ ] Subtask 3.3: Timer sıfırlandığında retro durumu `voting` olarak güncellenir; tüm notlar katılımcılara açılır
  - [ ] Subtask 3.4: "Aşamayı Bitir" butonu — moderatör manuel olarak aşamayı kapatabilir
  - [ ] Subtask 3.5: Timer mekanizması için testler yaz

---

## Dev Notes

**Relevant files:**

- `src/app/(app)/retro/new/page.tsx` — Retro oluşturma sayfası (oluşturulacak)
- `src/app/(app)/retro/[id]/page.tsx` — Aktif retro board sayfası (oluşturulacak)
- `src/app/api/retros/route.ts` — Retro CRUD API (oluşturulacak)
- `src/app/api/retros/[id]/notes/route.ts` — Sticky not API (oluşturulacak)
- `src/lib/mongodb/client.ts` — MongoDB bağlantısı
- `src/contexts/auth-context.tsx` — Auth state

**Patterns to follow:**

- MongoDB bağlantısı için `src/lib/mongodb/client.ts` kullan
- API route'larda Zod ile input validation yap
- Auth guard için `(app)/layout.tsx` pattern'ını takip et
- UI primitifleri için Ant Design kullan (`Form`, `Button`, `Input`, `Card`)

**API / Data:**

- `POST /api/retros` — `{ name, timerMinutes }` → `{ retroId, joinLink }`
- `GET /api/retros/[id]` — Retro durumu + notlar (aşamaya göre filtrelenmiş)
- `POST /api/retros/[id]/notes` — `{ category: 'mad'|'sad'|'glad', content }` → not oluşturur
- `PATCH /api/retros/[id]/phase` — `{ phase: 'writing'|'voting'|'grouping'|'actions' }` → aşama günceller

**MongoDB Model — Retro:**
```
{
  _id, name, timerMinutes, timerEndsAt, phase, createdBy,
  joinLink, createdAt
}
```

**MongoDB Model — Note:**
```
{
  _id, retroId, category, content, anonymousId, votes: [], createdAt
}
```

**Environment variables needed:**

- `MONGODB_URI` — MongoDB Atlas bağlantı stringi

---

## File List

> Dev subagent doldurur.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 14 Mayıs 2026 | 1.0 | Initial story | analyst |
