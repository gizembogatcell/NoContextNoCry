# Story 2: AI Gruplama + Dot Voting

**Status**: Approved
**Priority**: Must
**Epic**: Retro Board
**Created**: 14 Mayıs 2026

---

## Story

**As a** moderatör,
**I want** timer bittikten sonra sticky notları AI ile otomatik kümeletirebilmek ve ekibin dot voting yapmasını sağlayabilmek,
**so that** en önemli sorunlar öne çıksın ve aksiyonlar öncelikli konulara odaklanabilsin.

---

## Acceptance Criteria

1. Timer bittikten sonra moderatör "AI ile Grupla" butonuna basabilir; AI benzer notları kümeler ve her kümeye başlık önerir.
2. Moderatör küme başlıklarını düzenleyebilir ve notları farklı kümeler arasında sürükleyebilir (manuel düzenleme).
3. Gruplama tamamlandıktan sonra moderatör oylama aşamasını başlatır.
4. Her katılımcı toplam 3 oy hakkına sahiptir; oylarını tek bir kümeye veya birden fazlasına dağıtabilir.
5. Oylama anonim yapılır — kim hangi kümeye oy verdiği gizli kalır, sadece toplam oy sayısı görünür.
6. Moderatör oylama aşamasını kapatabilir; en çok oy alan kümeler sıralanmış şekilde gösterilir.
7. Edge case: AI gruplama 10 saniye içinde yanıt vermezse kullanıcıya hata mesajı gösterilir ve manuel gruplama seçeneği sunulur.

---

## Tasks / Subtasks

- [ ] Task 1: AI gruplama servisi (AC: 1)
  - [ ] Subtask 1.1: `POST /api/retros/[id]/group` endpoint — retrodaki tüm notları OpenAI API'ye gönderir, kümeleri ve başlıkları döner
  - [ ] Subtask 1.2: OpenAI prompt: "Aşağıdaki sticky notları anlamsal benzerliğe göre grupla, her gruba Türkçe kısa bir başlık ver" formatında
  - [ ] Subtask 1.3: Gruplar MongoDB'ye `NoteGroup` koleksiyonuna kaydedilir
  - [ ] Subtask 1.4: AI gruplama için testler yaz (mock OpenAI response)
- [ ] Task 2: Küme düzenleme UI (AC: 2)
  - [ ] Subtask 2.1: Küme başlığı inline düzenleme
  - [ ] Subtask 2.2: Notları kümeler arasında taşıma (drag-drop veya dropdown select)
  - [ ] Subtask 2.3: Düzenleme kaydetme `PATCH /api/retros/[id]/groups`
- [ ] Task 3: Dot voting mekanizması (AC: 3, 4, 5, 6)
  - [ ] Subtask 3.1: `POST /api/retros/[id]/votes` — `{ groupId, anonymousId }` → oy kaydeder; kullanıcı başına max 3 oy kontrolü
  - [ ] Subtask 3.2: Her küme üzerinde oy sayacı gösterilir; kimin oyladığı gizlenir
  - [ ] Subtask 3.3: Moderatör oylama aşamasını kapatır; kümeler oy sayısına göre sıralanır
  - [ ] Subtask 3.4: Voting için testler yaz

---

## Dev Notes

**Relevant files:**

- `src/app/(app)/retro/[id]/page.tsx` — Retro board (Story 1'de oluşturuldu)
- `src/app/api/retros/[id]/group/route.ts` — AI gruplama API (oluşturulacak)
- `src/app/api/retros/[id]/votes/route.ts` — Oylama API (oluşturulacak)
- `src/services/openai.ts` — OpenAI servis katmanı (oluşturulacak)

**Patterns to follow:**

- OpenAI çağrısı server-side API route içinden yapılır, client'a API key expose edilmez
- Story 1'deki polling pattern'ı (5 sn) oylama durumunu takip etmek için kullanılır

**API / Data:**

- `POST /api/retros/[id]/group` → `{ groups: [{ title, noteIds }] }`
- `PATCH /api/retros/[id]/groups` — `{ groups }` → günceller
- `POST /api/retros/[id]/votes` — `{ groupId }` → oy kaydeder
- `GET /api/retros/[id]/groups` → kümeler + oy sayıları

**MongoDB Model — NoteGroup:**
```
{
  _id, retroId, title, noteIds: [], voteCount, createdAt
}
```

**MongoDB Model — Vote:**
```
{
  _id, retroId, groupId, anonymousId, createdAt
}
```

**Environment variables needed:**

- `OPENAI_API_KEY` — OpenAI API anahtarı

---

## File List

> Dev subagent doldurur.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 14 Mayıs 2026 | 1.0 | Initial story | analyst |
