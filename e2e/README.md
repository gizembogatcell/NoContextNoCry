# E2E tests (Playwright)

Add `smoke.spec.ts` (or more) here after following **`docs/e2e-playwright.md`**.

Runs are driven by subagent **`#7 browser-e2e`** (`*e2e` / `*smoke`), not **#6 tester**.

Until Playwright is installed, keep this folder empty or only this README so CI does not run broken imports.
